#include <stdio.h>
int main()
{
    int m, i;
     printf("Enter an integer: ");
    scanf("%d",&n);

    for(i=1; i<=10; ++i)
    {
        printf("%d * %d = %d \n", m, i, n*i);
    }
    
    return 0;
}
